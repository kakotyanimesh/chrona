import localFont from "next/font/local";

export const arrayRegular = localFont({
    src: "../font/Array-Regular.otf",
    display: "swap",
    variable: "--array-font",
});

export const monofur = localFont({
    src: "../font/monof.ttf",
    display: "swap",
    variable: "--monof-font",
});
